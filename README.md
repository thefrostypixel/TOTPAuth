## [TOTPAuth](https://tfp.vitrem.org/TOTPAuth)

A local-first TOTP (Time-based One-Time Password) manager. It handles authentication token generation and storage entirely within the browser’s local storage.
